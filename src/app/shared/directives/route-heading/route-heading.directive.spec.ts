import { RouteHeadingDirective } from './route-heading.directive';

describe('RouteHeadingDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteHeadingDirective();
    expect(directive).toBeTruthy();
  });
});
